// 只需判断这个地址和签名商的地址一样不一样就行
// const sig = demoWallet.signingKey.sign(ethers.id("test"));
// console.log(sig);
// console.log(ethers.recoverAddress(ethers.id("test"), sig));

// const sig = demoWallet.signingKey.sign(ethers.id("test"));
// console.log(sig);
// console.log(ethers.recoverAddress(ethers.id("test"), sig.serialized));
// console.log(demoWallet.address);
